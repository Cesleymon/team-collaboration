def add_cards(x):
  if x== 'yes': #if x is yes, deal one card
    player_hand+=deal_card(deck)
      if player_score>21:
        return 'bust'
      else: 
        return player_turn
  if x==no:
    return score
  pass


def player_turn():
  if player_score>21:
    return 'Bust' # if score is over 21 its a bust
  if player_score==21:
    return 'winner' # is score is 21 they won
  if player_score<21:
    return 'use function add_cards, (yes) or (no)' # this will bring them to the function to add card or stop

